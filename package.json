{
  "name": "@platform/shared-utils",
  "version": "1.0.0",
  "description": "Shared, framework-agnostic utility functions for the White Label Platform",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
